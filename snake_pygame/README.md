Snake implementation using a linked-list to store the body locations.

# Run on Linux

    mkdir pygame_snake
    cd pygame_snake
    
    python -m venv .venv
    source .venv/bin/activate

    pip install pygame

    python app.py